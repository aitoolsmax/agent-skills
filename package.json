{
  "name": "@aitoolsmax/agent-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Portable agent skills and skills.sh-first publishing tools for AI Tools Max Apify Actors.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "skill": "node scripts/skill-cli.mjs",
    "validate": "node scripts/skill-cli.mjs validate all",
    "build": "node scripts/skill-cli.mjs build all",
    "test": "node --test",
    "check": "npm run validate && npm run build && npm test"
  },
  "license": "Apache-2.0"
}
